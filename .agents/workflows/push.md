---
description: Auto push zmian do GitHuba
---
// turbo-all

1. Dodaj wszystkie zmienione pliki do git:
```bash
git add .
```

2. Zrób commit z opisem zmian:
```bash
git commit -m "aktualizacja"
```

3. Wyślij na GitHuba:
```bash
git push origin main
```
